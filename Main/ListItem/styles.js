import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    listItem: {
        padding: 15,
        margin: 5,
        backgroundColor: "#333",
        flexDirection: "row",
    },
    listItemText : {
        color: "#fff",
    },
    listItemIndex : {
        color: "#AAA",
        marginRight: 10,
    },
});

export default styles;